services:
  backend:
    build:
      context: ./backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGODB_URI=mongodb://mongo:27017/fileinventory
      - ADMIN_NAME=System Administrator
    volumes:
      - ./backend/uploads:/usr/src/app/uploads
    depends_on:
      - mongo

  mongo:
    image: mongo:7.0
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:

volumes:
  mongo-data:
>>>>>>> b61312e66f1e98cbc77f4e3e2bc7db306358ba5f
