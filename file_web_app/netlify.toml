[build]
  base = "file_web_app"
  publish = "dist"
  command = "npm install && npm run build"

[dev]
  command = "npm run dev"
  port = 4173

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
